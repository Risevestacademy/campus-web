{
  "paths": [
    {
      "name": "react",
      "importNames": ["useEffect"],
      "message": "Components must derive state, use event handlers, or use a focused reusable hook instead of importing useEffect directly."
    }
  ]
}
